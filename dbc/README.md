# Server DBC set — WotLK 3.3.5a (build 12340)

Snapshot of the Ascension/Ebonhold server `dbc/` folder (246 current DBCs).
Retroport display rows merged into the live tables by Loom.MultiTool:

- CreatureDisplayInfo.dbc / CreatureModelData.dbc — DH Metamorphosis displays:
    80070 dhmaledps   80071 dhfemaledps   80072 dhmaletank   80073 dhfemaletank

These are the canonical merge targets for the retroport pipeline.
